import 'package:flutter/material.dart';

class DyscalculicPage extends StatelessWidget {
  const DyscalculicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('Dyscalculic'),
    ));
  }
}
